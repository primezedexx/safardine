"use client";

import { useEffect, useRef } from "react";
// import OneSignal from "react-onesignal";

export default function OneSignalProvider({
  userId,
}: {
  userId?: string | null;
}) {
  // OneSignal has been completely disabled per user request.
  return null;
}
